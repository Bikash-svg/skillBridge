// Single place for app-wide tunable constants that both the backend logic
// and the frontend (via GET /api/config) need to agree on.

module.exports = {
  MASTERY_THRESHOLD: 80, // percent -- see UPGRADE-V2.md §6
};
